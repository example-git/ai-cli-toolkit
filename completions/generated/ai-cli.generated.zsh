#compdef ai-cli

# AUTOMATICALLY GENERATED by `shtab`


_shtab_ai_cli_commands() {
  local _commands=(
    
  )
  _describe 'ai-cli commands' _commands
}

_shtab_ai_cli_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
)

# guard to ensure default positional specs are added only once per session
_shtab_ai_cli_defaults_added=0


_shtab_ai_cli() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*' default='*::: :->ai-cli'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_ai_cli_defaults_added )); then
    if (( ${_shtab_ai_cli_options[(I)${(q)one_or_more}*]} +          ${_shtab_ai_cli_options[(I)${(q)remainder}*]} +          ${_shtab_ai_cli_options[(I)${(q)default}]} == 0 )); then
      _shtab_ai_cli_options+=(': :_shtab_ai_cli_commands' '*::: :->ai-cli')
    fi
    _shtab_ai_cli_defaults_added=1
  fi
  _arguments -C -s $_shtab_ai_cli_options

  case $state in
    ai-cli)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_ai_cli-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_ai_cli -N ai-cli
else
  # autoload from fpath, call function directly
  _shtab_ai_cli "$@"
fi
